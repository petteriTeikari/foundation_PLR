# Foundation PLR Environment Variables
# =====================================
# Copy this file to .env and customize for your system:
#   cp .env.example .env
#
# These variables override hardcoded paths, enabling the codebase
# to work on any machine (CI/CD, Docker, different developers).

# =============================================================================
# MLflow Configuration
# =============================================================================

# Directory containing MLflow experiment runs
MLRUNS_DIR=/path/to/mlruns

# MLflow experiment ID for classification experiments
CLASSIFICATION_EXP_ID=253031330985650090

# =============================================================================
# Data Paths
# =============================================================================

# Path to the SERI PLR Glaucoma database (raw data)
SERI_DB_PATH=/path/to/SERI_PLR_GLAUCOMA.db

# Path to the extracted results database (generated by extraction)
FOUNDATION_PLR_RESULTS_DB=/path/to/foundation_plr_results.db

# Path to preprocessed signals database (for decomposition)
PREPROCESSED_SIGNALS_DB=/path/to/preprocessed_signals_per_subject.db

# =============================================================================
# Optional: Development Paths
# =============================================================================

# Manuscript directory (for figure output)
# MANUSCRIPT_DIR=/path/to/manuscripts/foundationPLR

# Alternative figures output directory
# FIGURES_OUTPUT_DIR=/path/to/figures/generated
